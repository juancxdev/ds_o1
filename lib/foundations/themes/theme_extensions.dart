/// Theme extensions for O1 Design System
/// Use this file to add custom theme extensions if needed in the future
class O1ThemeExtensions {
  O1ThemeExtensions._();

  // Example of how to add custom theme extensions:
  // static ThemeExtension<CustomColors> customColors = CustomColors(...);
}
